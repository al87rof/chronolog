# Chronolog

A Symfony-based backend application.

**Sports events calendar and results management system for timed competitions**
## Setup

```bash
composer install
cp .env.example .env
php bin/console doctrine:migrations:migrate